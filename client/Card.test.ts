import { render, screen } from '@testing-library/svelte';
// import Card from './about/_Card.svelte';

test('says fishing', () => {
	// render(Card, { props: { task: 'Fishing' } });
	// const node = screen.queryByText('Fishing');
	// expect(node).not.toBeNull();
});
