image:
  name: node:latest
  entrypoint: ['']

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - mv build public
    - sed -i "s|/_app/|./_app/|g" public/index.html
  artifacts:
    paths:
      - public
  tags:
    - docker
  only:
    - gitlab_pages
