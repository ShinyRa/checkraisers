stages:
  - install
  - build
  - deploy

install:
  stage: install
  script:
    yarn
  artifacts: 
    paths:
      - node_modules
  only:
    changes:
      - package-lock.json
    
build:
  stage: build
  dependencies: 
      - node_modules
  script:
    - yarn build
  artifacts:
    paths:
      - build
  only:
    - development

deploy:
  stage: deploy
  dependencies: 
    - build
  script:
    - node index.js
  only:
    - development
